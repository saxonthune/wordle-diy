import DiyContainer from "@/components/diyContainer";

export default function Home() {
  return (
      <main className="h-full flex"
        style={{ height: 'calc(var(--vh, 1vh)' }}
      >
        <DiyContainer/>
    </main>
  );
}

